apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: ex-hostpath
  name: ex-hostpath
spec:
  containers:
  - command:
    - sh
    - /scripturi/hello.sh
    image: alpine
    name: ex-hostpath
    volumeMounts:
    - mountPath: /scripturi
      name: scripturi
      readOnly: true
  volumes:
  - name: scripturi
    hostPath:
      path: /tmp/scripts/
